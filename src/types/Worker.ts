export default interface WorkerProps {
  departmentKey: number;
  departmentName: string;
  email: string;
  isWork: 'Y' | 'N';
  level: string;
  name: string;
  nickname: string;
  tel: string;
  workerKey: number;
}
